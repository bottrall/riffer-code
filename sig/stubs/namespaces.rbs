# Namespace modules autovivified by Zeitwerk in Ruby; RBS needs them declared
# explicitly so the compact `RifferCode::UI::*` / `RifferCode::Tools::*`
# signatures resolve.

module RifferCode
  module UI
  end

  module Tools
  end
end
